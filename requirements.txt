Django>=5.0,<6.0   # pinned below 6.0 — Django 6.x requires MySQL 8.4+, most installs are 8.0.x
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
django-filter>=24.1
python-decouple>=3.8
drf-spectacular>=0.27   # OpenAPI schema + Swagger/ReDoc docs

# MySQL driver — try mysqlclient first, it's faster.
mysqlclient>=2.2
# If mysqlclient fails to build (common on Windows without build tools),
# comment it out above and use this pure-Python fallback instead:
# pymysql>=1.1
