# Django
SECRET_KEY=change-this-to-a-random-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database: sqlite (default, no setup) | postgresql | mysql
DB_ENGINE=postgresql
DB_NAME=employee_management
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# JWT
JWT_ACCESS_MIN=60
JWT_REFRESH_DAYS=7
